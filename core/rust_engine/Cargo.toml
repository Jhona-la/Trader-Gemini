[package]
name = "quantum_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "quantum_engine"
crate-type = ["cdylib", "staticlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
