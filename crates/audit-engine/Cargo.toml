[package]
name = "audit-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
crossbeam = "0.8"
omniscient-registry = { path = "../omniscient-registry" }
phase-runner = { path = "../phase-runner" }
graph-4d = { path = "../graph-4d" }
