[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "quantum_bridge"
version = "0.1.0"
description = "Trader Gemini Quantum Bridge"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "quantum_bridge"
